<%= form_for(@app_role) do |f| %>
  <% if @app_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_role.errors.count, "error") %> prohibited this app_role from being saved:</h2>

      <ul>
      <% @app_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
